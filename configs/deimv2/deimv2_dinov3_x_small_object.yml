__include__: [
  '/data/wyh/DEIMv2/configs/dataset/custom_detection.yml',
  '../runtime.yml',
  '../base/dataloader.yml',
  '../base/optimizer.yml',
  '../base/deimv2.yml',
]


output_dir: ./outputs/deimv2_dinov3_x_small_object
  
DEIM:
  backbone: DINOv3STAs

DINOv3STAs:
  name: dinov3_vits16plus
  weights_path: ./ckpts/dinov3_vits16plus_pretrain_lvd1689m-4057cbaa.pth
  # Extract features from 4 layers for 4-scale feature pyramid
  # Use n parameter to specify which layers to extract (0-indexed)
  # layers 2,5,8,11 for stride 4,8, 16, 32
  interaction_indexes: [2, 5, 8, 11]
  finetune: True
  conv_inplane: 64
  hidden_dim: 256
  use_p2: True  # Enable P2 feature output (stride=4)
  
HybridEncoder:
  in_channels: [256, 256, 256, 256]  # 4-scale features
  feat_strides: [4, 8, 16, 32]  # P2, P3, P4, P5
  # intra
  hidden_dim: 256
  dim_feedforward: 1024

  # cross 
  expansion: 1.25
  depth_mult: 1.37
  
  # Small object enhancement
  use_p2: True
  use_small_object_enhancement: True  # Enable small object enhancement module

DEIMTransformer:
  num_layers: 6
  eval_idx: -1
  num_levels: 4  # 4-scale feature pyramid
  feat_channels: [256, 256, 256, 256]
  feat_strides: [4, 8, 16, 32]  # P2, P3, P4, P5
  # reg_scale: 8
  hidden_dim: 256
  dim_feedforward: 2048
  
  # 方案二：多尺度采样点优化 - P2层使用更多采样点
  num_points: [8, 4, 4, 4]  # P2: 8 points, P3/P4/P5: 4 points
  num_points_per_scale: [8, 4, 4, 4]  # 显式配置每层的采样点数
  
  cross_attn_method: default
  query_select_method: default
  activation: silu
  mlp_act: silu
  
  # ============================================
  # 小目标检测头增强配置（五个方案全部启用）
  # ============================================
  
  # 方案一：小目标感知查询选择
  use_small_object_head: True
  small_object_threshold: 0.1  # 面积小于0.1的视为小目标
  
  # 方案三：尺度自适应回归头
  use_scale_adaptive_reg: True
  reg_scales_per_level: [2.0, 4.0, 6.0, 8.0]  # P2/P3/P4/P5使用不同的回归范围
  
  # 方案五：IoU感知分类优化（增强LQE）
  use_enhanced_lqe: True

optimizer:
  type: AdamW
  lr: 0.0005
  betas: [0.9, 0.999]
  weight_decay: 0.000125

epoches: 96 #  Total epochs: 80 for training + EMA  for 4n = 12. n refers to the model size in the matched config.

flat_epoch: 44    # 4 + 20 // 2
no_aug_epoch: 16  # 4n


## Dense O2O: Mosaic + Mixup + CopyBlend
train_dataloader: 
  dataset: 
    transforms:
      ops:
        - {type: Mosaic, output_size: 320, rotation_range: 10, translation_range: [0.1, 0.1], scaling_range: [0.5, 1.5],
           probability: 1.0, fill_value: 0, use_cache: True, max_cached_images: 50, random_pop: True}
        - {type: RandomPhotometricDistort, p: 0.5}
        - {type: RandomZoomOut, fill: 0}
        - {type: RandomIoUCrop, p: 0.8}
        - {type: SanitizeBoundingBoxes, min_size: 1}
        - {type: RandomHorizontalFlip}
        - {type: Resize, size: [640, 640], }
        - {type: SanitizeBoundingBoxes, min_size: 1}
        - {type: ConvertPILImage, dtype: 'float32', scale: True}
        - {type: Normalize, mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
        - {type: ConvertBoxes, fmt: 'cxcywh', normalize: True}
      policy:
        epoch: [4, 44, 80]   # list 

  collate_fn:
    mixup_epochs: [4, 44]
    stop_epoch: 80
    copyblend_epochs: [4, 80]
    base_size_repeat: 3

val_dataloader:
  dataset:
    transforms:
      ops:
        - {type: Resize, size: [640, 640], }
        - {type: ConvertPILImage, dtype: 'float32', scale: True}
        - {type: Normalize, mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
